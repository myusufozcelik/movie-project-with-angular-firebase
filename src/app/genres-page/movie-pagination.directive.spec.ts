import { MoviePaginationDirective } from './movie-pagination.directive';

describe('MoviePaginationDirective', () => {
  it('should create an instance', () => {
    const directive = new MoviePaginationDirective();
    expect(directive).toBeTruthy();
  });
});
